apiVersion: v1
kind: Service
metadata:
  name: oip-sms-dev
spec:
  ports:
  - port: 80
    targetPort: 8080
  selector:
    app: oip-sms-dev
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: oip-sms-dev
spec:
  selector:
    matchLabels:
      app: oip-sms-dev
  replicas: 1
  template:
    metadata:
      labels:
        app: oip-sms-dev
    spec:
      containers:
      - name: oip-sms-dev
        image: socialalphaoip/sms-microservice:dev
        ports:
        - containerPort: 8080
---