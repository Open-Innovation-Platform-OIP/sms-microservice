apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: oip-sms-dev-ingress
  annotations:
    kubernetes.io/ingress.class: "nginx"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - oip-sms-dev.oip.dev.code-alpha.org
    secretName: oip-sms-dev-tls
  rules:  
  - host: oip-sms-dev.oip.dev.code-alpha.org
    http:
      paths:
      - backend:
          serviceName: oip-sms-dev
          servicePort: 80